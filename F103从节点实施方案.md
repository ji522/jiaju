# F103 从节点实施方案

## 目标

基于现有 F103 工程快速做出一个最小 CAN 从节点，用来和当前 F407 主节点形成真实双节点联调。

目标闭环：

```text
MQTT
 -> F407 主节点
 -> CAN BODY_CMD (0x100)
 -> F103 从节点
 -> 执行本地 LED
 -> CAN BODY_STATUS (0x101)
 -> F407 主节点
 -> /vehicle/bcm/status
```

---

## 设计原则

本版本的 F103 从节点只做最必要的事情：

1. 不联网
2. 不跑 MQTT
3. 不做复杂 UI
4. 只做：
   - 接收 `BODY_CMD`
   - 执行输出
   - 回发 `BODY_STATUS`
   - 可选读取本地按键并带回状态

这样联调最稳，也最容易快速出成果。

---

## 推荐起点

建议以现有 F103 代码副本为基础，而不是从零新开工程。

可参考的现有 F103 副本：

- [SmartHome - 副本](</D:/BaiduNetdiskDownload/项目资料/项目资料/项目代码/SmartHome/SmartHome - 副本>)

这个副本里已经有：

- `main.c`
- `app_led.c`
- `app_key.c`
- `driver_led_key.c/h`
- FreeRTOS 基础框架

这些都可以复用，减少重复劳动。

---

## F103 从节点建议保留的模块

### 保留

- `1_App/main.c`
- `1_App/app_led.c`
- `1_App/app_key.c`
- `6_ModuleDrives/driver_led_key.c`
- `6_ModuleDrives/driver_led_key.h`
- FreeRTOS 基础组件

### 新增

- `1_App/app_can_slave.c`
- `6_ModuleDrives/driver_can_slave.c`
- `6_ModuleDrives/driver_can_slave.h`

### 共享

- 复制/共用：
  [can_bcm_protocol.h](</D:/BaiduNetdiskDownload/项目资料/项目资料/项目代码/SmartHome/SmartHome_mqtt服务器用的实习这里的/6_ModuleDrives/can_bcm_protocol.h>)

这份协议头必须和 F407 保持一致。

---

## F103 从节点职责

### 1. CAN 接收

监听：

- `CAN_ID_BODY_CMD (0x100)`

收到后解析 Byte0：

- bit0 -> lamp
- bit1 -> hazard
- bit2 -> fan

### 2. 本地执行

最小版本只做：

- `lamp` 控制本地 LED

`hazard/fan` 暂时作为状态位保留，不一定必须接真实外设。

### 3. 状态反馈

执行完后立即回发：

- `CAN_ID_BODY_STATUS (0x101)`

建议帧格式：

- DLC = 2
- Byte0 = 当前输出状态位图
- Byte1 = 当前节点模式

### 4. 本地输入（可选增强）

后续可以让 F103 本地按键改变某个状态位，再通过 `BODY_STATUS` 反馈给 F407。

但第一阶段建议先不要引入更多变量，先把主闭环跑通。

---

## F103 节点模式建议

可直接复用协议头里的：

- `CAN_NODE_INIT`
- `CAN_NODE_NORMAL`
- `CAN_NODE_DEGRADED`
- `CAN_NODE_FAULT`

最小版本建议：

- 初始化期间：`INIT`
- CAN 工作正常：`NORMAL`
- 如果发送/接收失败次数连续超限：`DEGRADED`

---

## F103 硬件建议

### LED / KEY

优先复用现有 F103 副本里的 LED 和按键引脚：

- LED：
  [driver_led_key.h](</D:/BaiduNetdiskDownload/项目资料/项目资料/项目代码/SmartHome/SmartHome - 副本/6_ModuleDrives/driver_led_key.h:14>)
  当前是 `PA7`

- KEY：
  [driver_led_key.h](</D:/BaiduNetdiskDownload/项目资料/项目资料/项目代码/SmartHome/SmartHome - 副本/6_ModuleDrives/driver_led_key.h:31>)
  当前是 `PB11`

这样可以少改原来的外设驱动。

### CAN 引脚

F103 的 CAN1 常见两种方案：

1. 默认：
   - `PA11 = CAN_RX`
   - `PA12 = CAN_TX`

2. Remap：
   - `PB8 = CAN_RX`
   - `PB9 = CAN_TX`

建议优先根据你板子和 TJA 模块排针来选。

如果板子上 `PA11/PA12` 被 USB 占用，建议走 `PB8/PB9 remap`。

---

## F103 软件结构建议

### main.c

启动顺序建议：

1. `HAL_Init`
2. `SystemClock_Config`
3. `DBGOUT` 初始化
4. `LED task`
5. `KEY task`（如果保留）
6. `CAN slave task`
7. `vTaskStartScheduler`

### app_can_slave.c

建议实现一个任务：

- 初始化 CAN
- 通过 CAN RX 中断接收 `BODY_CMD`，主循环/任务消费软件队列
- 更新 LED 状态
- 回发 `BODY_STATUS`

### driver_can_slave.c

建议实现：

- `Driver_CAN_Slave_Init()`
- `Driver_CAN_Slave_SendStatus()`
- `Driver_CAN_Slave_RecvCmd()`

这样职责最清楚。

---

## 联调阶段建议

### 第一阶段：内部单板自测

先在 F103 单板上验证：

- 能初始化 CAN
- 能解析 `BODY_CMD`
- 能回发 `BODY_STATUS`

### 第二阶段：F407 + F103 实总线联调

联调顺序：

1. F407 切到 `CAN_LINK_MODE_NORMAL`
2. F103 上电进入从节点模式
3. 两边接 TJA 模块
4. 统一波特率
5. 发：
   ```json
   {"cmd":"bcm_ctrl","lamp":true,"hazard":false,"fan":false}
   ```
6. 观察：
   - F103 LED 是否点亮
   - F103 是否回 `BODY_STATUS`
   - F407 是否上报 `/vehicle/bcm/status`

---

## 当前最推荐的推进顺序

1. 先保留 F407 现有版本不动
2. 复制共享协议头 `can_bcm_protocol.h` 到 F103 工程可见位置
3. 写 F103 最小从节点
4. 再把 F407 从 `LOOPBACK` 切到 `NORMAL`
5. 做双节点联调

---

## 做成之后简历可以怎么写

做成双节点后，这个项目就可以自然表述成：

- `STM32F407` 作为主控 / 网关节点
- `STM32F103` 作为执行从节点
- 两节点通过 `CAN` 完成 `BODY_CMD -> BODY_STATUS` 控制闭环
- 主节点接入 MQTT，用于远程控制与状态上报

这会比单板 loopback 更有说服力，也更接近车企岗位的通信节点场景。
