# 问题记录与修复日志

> 说明：
> - 这个文件用于长期记录本项目遇到的问题、现象、定位过程、修复方案和当前结论。
> - 以后每次改代码解决了一个问题，都继续在这个文件末尾追加。
> - 目标不是只记“改了什么”，而是记清楚“为什么出问题、怎么证明、为什么这样改”。

---

## 2026-05-07 第一轮：F103 迁移到 F407 后 MQTT 不通

### 现象

- F103 原版 MQTT 能正常连接和收发。
- 迁移到 F407 后，MQTT 无法建立连接。
- 早期表现为：
  - ESP8266 初始化异常
  - WiFi 入网失败
  - TCP / MQTT 后续链路无法进入

### 根因

#### 1. USART2_RX(PA3) GPIO 模式配置不合适

- 文件：
  [driver_net.c](</D:/BaiduNetdiskDownload/项目资料/项目资料/项目代码/SmartHome/SmartHome_mqtt服务器用的实习这里的/6_ModuleDrives/driver_net.c:347>)
- 问题：
  - F407 迁移后 `PA3` 被配置成了不合适的模式。
  - 导致 ESP8266 回包接收链路不稳定。
- 修复：
  - 将 RX 引脚改回正常的 `AF_PP` 配置。

#### 2. ESP8266 复位后等待时间太短

- 文件：
  [driver_net.c](</D:/BaiduNetdiskDownload/项目资料/项目资料/项目代码/SmartHome/SmartHome_mqtt服务器用的实习这里的/6_ModuleDrives/driver_net.c:650>)
- 问题：
  - `AT+RST` 后只等 `500ms` 就继续发 `AT+CWMODE=1`
  - ESP8266 还没真正 ready
- 修复：
  - 延长为 `2000ms`

#### 3. WiFi 入网状态机判定过早失败

- 文件：
  [driver_net.c](</D:/BaiduNetdiskDownload/项目资料/项目资料/项目代码/SmartHome/SmartHome_mqtt服务器用的实习这里的/6_ModuleDrives/driver_net.c:218>)
- 问题：
  - `CWJAP` 过程中出现 `WIFI DISCONNECT` 时直接判失败
  - 但 ESP8266 在手机热点连接过程中中间出现一次 `WIFI DISCONNECT` 是可能的
- 修复：
  - 只把明确错误串作为失败
  - `WIFI DISCONNECT` 不再立刻判死

#### 4. WiFi 入网超时太短

- 文件：
  [MQTTFreeRTOS.c](</D:/BaiduNetdiskDownload/项目资料/项目资料/项目代码/SmartHome/SmartHome_mqtt服务器用的实习这里的/3_Protocol/MQTTClient-C/src/FreeRTOS/MQTTFreeRTOS.c:127>)
- 问题：
  - 原来只给 `5000ms`
  - 手机热点场景容易超时
- 修复：
  - 放宽到 `15000ms`

### 修复后验证结果

- 串口日志能够连续看到：
  - `Connect WiFi success`
  - `TCP connecting`
  - `MQTT Connected`
  - `Subscribe OK`
- 结论：
  - MQTT 不通的根因不在 MQTT 协议本身
  - 而在 F407 迁移后的 UART/ESP8266 初始化链路

---

## 2026-05-07 第二轮：MQTT 连接成功后运行态不稳定

### 现象

- MQTT 已经能连上
- 但进入 `RUNNING` 后偶尔会很快掉线，`MQTTYield()` 报错

### 原因与修复

#### 1. MQTT 读包对 `+IPD` 分片不够稳

- 文件：
  [MQTTClient.c](</D:/BaiduNetdiskDownload/项目资料/项目资料/项目代码/SmartHome/SmartHome_mqtt服务器用的实习这里的/3_Protocol/MQTTClient-C/src/MQTTClient.c>)
- 修复：
  - 重写剩余长度读取逻辑
  - 把短读更偏向当作“包未到齐”而不是“协议失败”

#### 2. MQTT 缓冲区偏小

- 文件：
  [app_mqtt.c](</D:/BaiduNetdiskDownload/项目资料/项目资料/项目代码/SmartHome/SmartHome_mqtt服务器用的实习这里的/1_App/app_mqtt.c>)
- 修复：
  - `sendbuf/readbuf` 从 `256` 扩到 `512`
  - `MQTTYield` 调整为更稳的轮询参数

### 结论

- 这一轮主要解决的是“连上之后的稳定性”
- 属于协议栈和传输层协同问题，不是 MQTT 服务器问题

---

## 2026-05-07 第三轮：加 CAN 后任务创建成功，但实际不运行

### 现象

- 串口只能看到：
  - `Create MQTT Task success.`
  - `Create LED Task success.`
  - `Create Key Task success.`
  - `Create CAN Task success.`
- 但看不到任务内部日志：
  - `[MQTT] Task started`
  - `[CAN] Task started`
  - `[KEY] Task started`

### 根因

#### 1. FreeRTOS 仍然保留了 F103 的时钟配置

- 文件：
  [FreeRTOSConfig.h](</D:/BaiduNetdiskDownload/项目资料/项目资料/项目代码/SmartHome/SmartHome_mqtt服务器用的实习这里的/4_MiddleWare/FreeRTOSConfig.h>)
- 问题：
  - `configCPU_CLOCK_HZ` 仍是 `72MHz`
  - `configSYSTICK_CLOCK_HZ` 仍是 F103 风格配置
- 修复：
  - 改为适配 F407 的 `168MHz`

#### 2. FreeRTOS heap 太小

- 文件：
  [FreeRTOSConfig.h](</D:/BaiduNetdiskDownload/项目资料/项目资料/项目代码/SmartHome/SmartHome_mqtt服务器用的实习这里的/4_MiddleWare/FreeRTOSConfig.h>)
- 问题：
  - 原来 heap 只有 `10240`
  - 增加了 CAN task、Diag task、MQTT 缓冲增大后，不够用了
- 修复：
  - heap 提高到 `24576`

### 修复后验证结果

- 日志开始正常出现：
  - `[MAIN] About to start scheduler, free heap=...`
  - `[MQTT] Task started`
  - `[CAN] Task started`
  - `[KEY] Task started`
- 说明调度器和任务上下文已经真实运行

---

## 2026-05-07 第四轮：CAN 接收路径设计混乱

### 现象

- 加入 CAN 后，设计上同时存在：
  - 轮询收包
  - RX0 中断通知
- 但对应的中断处理链路并不完整

### 问题点

- 文件：
  [driver_can.c](</D:/BaiduNetdiskDownload/项目资料/项目资料/项目代码/SmartHome/SmartHome_mqtt服务器用的实习这里的/6_ModuleDrives/driver_can.c>)
- 问题：
  - 收包逻辑想走轮询
  - 但又打开了 RX 中断通知
  - 风格混用，增加不确定性

### 修复

- 当前版本先统一成：
  - **只走轮询接收**
  - 不混用 RX 中断通知

### 说明

- 这不是最终双节点总线版本
- 是当前 loopback 原型阶段更稳妥的选择

---

## 2026-05-07 第五轮：测试时误发 MQTT topic，导致误判“无响应”

### 现象

- 发了 JSON 命令后，板子不响应
- 误以为是代码没工作

### 根因

- 控制命令误发到了：
  - `/vehicle/body/status`
- 但代码真正订阅的控制入口是：
  - `/vehicle/body/cmd`
  - 后续收口后是 `/vehicle/bcm/command`

### 结论

- 属于测试方法问题，不是功能本身失效

---

## 2026-05-07 第六轮：LED 闪一下又灭

### 现象

- 发送 `body_ctrl` 后
- LED 会先亮一下，但很快又灭

### 根因

- 同一颗 LED 被同时用于：
  - 表示执行器状态（`lamp`）
  - CAN 调试收发闪灯

### 修复

- 文件：
  [app_can.c](</D:/BaiduNetdiskDownload/项目资料/项目资料/项目代码/SmartHome/SmartHome_mqtt服务器用的实习这里的/1_App/app_can.c>)
- 处理：
  - 去掉 CAN 收发路径里的 `LED_SHINE()`
  - 现在 LED 只表示 `lamp` 状态

### 结果

- `lamp=true` 时 LED 稳定常亮
- `lamp=false` 时 LED 熄灭

---

## 2026-05-07 第七轮：BCM 控制命令响应很慢

### 现象

- 发送：
  ```json
  {"cmd":"body_ctrl","lamp":true,"hazard":false,"fan":true}
  ```
- LED 不是立刻变化
- 有时要等几秒甚至十几秒

### 根因

- 文件：
  [app_can.c](</D:/BaiduNetdiskDownload/项目资料/项目资料/项目代码/SmartHome/SmartHome_mqtt服务器用的实习这里的/1_App/app_can.c>)
- 问题：
  - 为了降低心跳噪声，曾经把 CAN 任务主循环周期直接拉成了心跳周期
  - 结果 `xCanTxQueue` 的控制命令也要等 CAN 任务下一轮才处理

### 修复

- CAN 任务拆成两个周期：
  - 任务轮询周期：`20ms`
  - 心跳发送周期：`5000ms`
- 控制命令快速处理，心跳独立慢周期发送

### 结果

- BCM 控制命令响应恢复正常
- 心跳保活仍然保留，但不再拖慢控制链路

---

## 2026-05-07 第八轮：项目语义从 SmartHome 收口为 BCM 节点原型

### 目标

- 让项目从“智能家居 + CAN 测试”收口为更像车企方向的：
  - `BCM（Body Control Module）` 节点原型
  - `MQTT-CAN` 轻量网关原型

### 当前 topic 语义

- 主控制 topic：
  - `/vehicle/bcm/command`
- 主状态 topic：
  - `/vehicle/bcm/status`
- 主输入 topic：
  - `/vehicle/bcm/input`

兼容保留：
- `/vehicle/body/cmd`
- `/smarthome/led/cmd`
- `/smarthome/key/info`

### 当前能力

- `bcm_ctrl/body_ctrl` 命令能够转为 CAN `BODY_CMD`
- 节点执行后回 `BODY_STATUS`
- MQTT 上报 `node_status` 与 `body_status`
- 按键通过 `/vehicle/bcm/input` 上报，同时保留旧 topic

### 当前边界

- 当前 CAN 仍为 `CAN_MODE_LOOPBACK`
- 还不是双节点真实总线联调版本
- `hazard/fan` 目前仅为状态位，未接真实外设

---

## 后续追加规则

以后每解决一个问题，请按下面格式继续加：

```md
## YYYY-MM-DD 第N轮：问题标题

### 现象
- ...

### 根因
- ...

### 修复
- 文件：
  [xxx.c](绝对路径)
- 改动：
  - ...

### 验证结果
- ...

### 结论
- ...
```

